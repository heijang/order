spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
  h2:
    console:
      enabled: true
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
#    hibernate:
#      ddl-auto: create

server:
  shutdown: graceful

cloud:
  aws:
    access-key: access-key
    secret-key: secret-key